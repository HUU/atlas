import { describe, expect, test } from 'vitest';

describe('integration test placeholder', () => {
  test('put integration tests in this folder (delete this)', () => {
    expect(true).toBe(true);
  });

  test('put unit tests next to the sourceFile.ts as sourcefile.unit.test.ts', () => {
    expect(true).toBe(true);
  });
});
