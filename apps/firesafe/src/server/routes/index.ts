export default eventHandler((_event) => {
  return 'Start by editing <code>server/routes/index.ts</code>.';
  // use something like https://github.com/michealroberts/trpc-nitro-adapter/blob/main/src/defineNitroTRPCEventHandler.ts to mount ts-rest endpoints in Nitro
});
